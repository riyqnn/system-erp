// Prisma Schema untuk Project-4B
// Database schema akan didefinisikan di sini

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models akan ditambahkan di sini untuk setiap modul:
// - Inventory (Item, Stock, Warehouse, etc.)
// - Finance (Transaction, Account, etc.)
// - Purchasing (PurchaseOrder, Supplier, etc.)
// - Production (BOM, ProductionOrder, etc.)
// - SNM (SalesOrder, Customer, etc.)
